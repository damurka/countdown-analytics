% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Run the Pooled app}
\usage{
run_app(
  pre_loaded_dir = Sys.getenv("CDSUITE_SHINY_SELECTED_FILE", unset = NA),
  language = Sys.getenv("CDSUITE_SHINY_LOCALE", unset = "en"),
  app_name = Sys.getenv("CDSUITE_SHINY_NAME", unset = "Pooled"),
  app_version = Sys.getenv("CDSUITE_SHINY_VERSION", unset =
    as.character(utils::packageVersion("cd2030.pooled")))
)
}
\arguments{
\item{pre_loaded_dir}{A folder of saved datasets (\code{.rds}) to start from, or \code{NA}.}

\item{language}{The starting language: \code{"en"}, \code{"fr"} or \code{"pt"}.}

\item{app_name, app_version}{The name and version shown in the header.}
}
\value{
A Shiny app object: printing it (or \code{\link[shiny:runApp]{shiny::runApp()}}) runs it.
}
\description{
Build one file from several countries' saved datasets, then explore it and export tables. DataSuite starts it with
its settings in environment variables (\code{CDSUITE_SHINY_NAME}, \verb{_VERSION}, \verb{_LOCALE}, and \verb{_SELECTED_FILE}: a folder
of saved datasets); they can be given as arguments instead.
}
