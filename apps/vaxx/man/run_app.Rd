% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Run the Vaxx app}
\usage{
run_app(
  selected_file = Sys.getenv("CDSUITE_SHINY_SELECTED_FILE", unset = NA),
  language = Sys.getenv("CDSUITE_SHINY_LOCALE", unset = "en"),
  app_name = Sys.getenv("CDSUITE_SHINY_NAME", unset = "Vaxx"),
  app_version = Sys.getenv("CDSUITE_SHINY_VERSION", unset =
    as.character(utils::packageVersion("cd2030.vaxx"))),
  ...
)
}
\arguments{
\item{selected_file}{A dataset to open (DataSuite's \code{CDSUITE_SHINY_SELECTED_FILE}), or \code{NA}.}

\item{language}{The starting language: \code{"en"}, \code{"fr"} or \code{"pt"}.}

\item{app_name, app_version}{The name and version shown in the header.}

\item{...}{Passed to \code{\link[shiny:shinyApp]{shiny::shinyApp()}}'s \code{options} (e.g. \code{port}, \code{launch.browser}).}
}
\value{
A Shiny app object: printing it (or \code{\link[shiny:runApp]{shiny::runApp()}}) runs it.
}
\description{
The Countdown Vaxx app: the Load Data wizard, the data quality, denominator and analysis pages, and the report
builder. DataSuite starts it with its settings in environment variables (\code{CDSUITE_SHINY_NAME}, \verb{_VERSION}, \verb{_LOCALE},
\verb{_SELECTED_FILE}); they can be given as arguments instead.
}
