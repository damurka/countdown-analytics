Shared page modules: the ones both apps had word for word. They read nothing that is specific to one indicator group
(indicator lists come from cd2030.core / the app's options), so an app on any group -- rmncah, vaccine or a custom
one -- uses them as they are. Anything that differs by app stays in that app's own modules/ folder.
